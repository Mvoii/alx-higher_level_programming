// select the elemnt
const element = document.querySelector('.header');

// check if found
if (element) {
    // set colour
    element.style.color = 'red';
} else {
    console.log('Element not found');
};
